export function verifyIfPreviouslyConvertedImage(
  image,
  width: number,
  height: number
) {
  let isPreviouslyVerified = false;

  return isPreviouslyVerified;
}

export function retrieveCachedImage(image, width, height);
